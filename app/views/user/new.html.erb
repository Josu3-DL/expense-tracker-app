<!DOCTYPE html>
<html lang="es">
    
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Expense Tracker - Crear Cuenta</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

    <%= stylesheet_link_tag "styles", media: "all" %>
 
</head>

<body>
    <div class="container-fluid p-0">
        <div class="row g-0">
            <div class="col-md-6 login-form-container">
                <div class="login-form p-4 p-md-5">
                    <div class="brand mb-4">
                        <div class="icon-circle">
                            <span class="dollar-icon">$</span>
                        </div>
                        <span class="brand-name ms-2">Expense Tracker</span>
                    </div>
                    
                    <h1 class="welcome-text">Crear Cuenta</h1>
                    <p class="subtitle mb-5">Registrate para comenzar a gestionar tus gastos</p>
                    
                      <%= form_with(model: @user, url: register_path, local: true, html: {id: "registerForm"}) do |f| %>

                        <% if flash[:alert] %>
                        <div class="alert alert-danger" role="alert">
                            <ul>
                                <% flash[:alert].each do |error| %>
                                    <li class="form-label"> <%= error %> </li>
                                <% end %>
                            </ul>
                        </div>
                        <% end %>
                        
                        <div class="mb-3">
                            <%= f.label :username, "Nombre de usuario", class: "form-label"  %>
                            <%= f.text_field :username, class: "form-control", placeholder: "Escribe tu Usuario", id: "username", required: true %>
                        </div>
                        <div class="mb-3">
                            <%= f.label :password, "Contraseña", class: "form-label" %>
                            <%= f.password_field :password, class: "form-control", placeholder: "Escribe tu Contraseña", id: "password", required: true%>
                        </div>
                        <div class="mb-3">
                            <%= f.label :password_confirmation, "Confirmar contraseña", class: "form-label" %>
                            <%= f.password_field :password_confirmation, class: "form-control", placeholder: "Confirmar Contraseña", id: "confirmPassword", required: true%>
                        </div>
                        <%= f.submit "Registrarse", class: "btn btn-primary w-100 login-btn" %>
                    <% end %>
                    
                    <div class="register-link mt-4 text-center">
                        ¿Ya tienes una cuenta? <%= link_to "Inicia sesion", login_path, class: "register-text" %>
                    </div>
                </div>
            </div>
            
            <div class="col-md-6 info-banner">
                <div class="banner-content text-center">
                    <div class="banner-icon mb-3">$</div>
                    <h2 class="banner-title">Toma el control de tus finanzas</h2>
                    <p class="banner-text">
                        Registrarte te permitirá comenzar a rastrear tus gastos, establecer presupuestos y alcanzar tus metas financieras.
                    </p>
                </div>
            </div>
        </div>
    </div>
  
</body>
</html>

